import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  settingsButton: {
    position: 'absolute',
    top: 12,
    left: 12,
    zIndex: 50
  },
}));
